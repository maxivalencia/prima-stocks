<form method="post" action="{{ path('piece_jointe_delete', {'id': piece_jointe.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece_jointe.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
