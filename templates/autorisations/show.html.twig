{% extends 'base.html.twig' %}

{% block title %}Autorisation{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Autorisation</h1>
    </div>
    <hr>

    <table class="table">
        <tbody>
            <tr>
                <td>Id</td>
                <td>{{ autorisation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autorisations_index') }}" class="btn btn-primary">Retour</a>
    <br><br>
    <a href="{{ path('autorisations_edit', {'id': autorisation.id}) }}" class="btn btn-warning">Edition</a>
    <br><br>
    {{ include('autorisations/_delete_form.html.twig') }}
</div>
{% endblock %}
