{% extends 'base.html.twig' %}

{% block title %}New Username{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Création nouveau Username</h1>
    </div>
    <hr>

    <br /><br />

    {{ include('username/_form.html.twig') }}
    <br>
    <a href="{{ path('username_index') }}" class="btn btn-primary">Retour</a>
</div>
{% endblock %}
